export interface YouTubeList {
    id: string;
    name: string;
    thumbnail: string;
    channel: string;
}

export interface YouTubeChannel {
    id: string;
    name: string;
    thumbnail: string;
}